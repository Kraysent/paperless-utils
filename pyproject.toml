[project]
name = "paperless-utils"
version = "0.1.0"
description = "Reads document notifications from Redis streams and sends Telegram alerts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0.0",
    "pydantic>=2.0.0",
    "httpx>=0.25.0",
    "python-dotenv>=1.0.0",
]
